# =========================================================================
# BackupDB Configuration Sample - Version 6.7
# =========================================================================
# Copy this file to BackupDB.env and configure your settings
# The script automatically loads: ./BackupDB.env or $HOME/BackupDB.env
# =========================================================================

# Incremental backups: skip unchanged databases (default: true)
VGX_DB_INCREMENTAL_BACKUPS="true"

# =========================================================================
# CORE DATABASE SETTINGS (REQUIRED)
# =========================================================================
VGX_DB_HOSTS="localhost,db2.example.com"
VGX_DB_PORTS="3306,3306"
VGX_DB_USERS="backup_user,backup_user"
VGX_DB_PASSWORDS="password1,password2"

# Local backup directory (default: $HOME/DBBackup/)
VGX_DB_OPATH="$HOME/DBBackup/"

# =========================================================================
# STORAGE BACKEND (Choose one: git, s3, onedrive)
# =========================================================================
VGX_DB_STORAGE_TYPE="git"

# =========================================================================
# GIT SETTINGS (when VGX_DB_STORAGE_TYPE="git")
# =========================================================================
VGX_DB_GIT_REPO="git@github.com:username/backup-repo.git"

# Git backup retention: -1=never delete, 0=delete all old, 30=keep 30 days
VGX_DB_GIT_RETENTION_DAYS="-1"

# =========================================================================
# S3 SETTINGS (when VGX_DB_STORAGE_TYPE="s3")
# =========================================================================
# Works with: AWS S3, Backblaze B2, Wasabi, DigitalOcean Spaces, MinIO

AWS_ACCESS_KEY_ID="your-access-key"
AWS_SECRET_ACCESS_KEY="your-secret-key"
VGX_DB_S3_BUCKET="your-backup-bucket"

# Required for non-AWS services:
VGX_DB_S3_ENDPOINT_URL="https://s3.us-west-004.backblazeb2.com"

# Optional S3 settings:
# VGX_DB_S3_REGION="us-west-004"
# VGX_DB_S3_PREFIX="backups/"

# =========================================================================
# ONEDRIVE SETTINGS (when VGX_DB_STORAGE_TYPE="onedrive")
# =========================================================================
# Requires rclone configuration first: rclone config

ONEDRIVE_REMOTE="onedrive"
ONEDRIVE_PATH="/DatabaseBackups"

# =========================================================================
# CLEANUP & OPTIONAL SETTINGS
# =========================================================================
# Delete local files after upload (default: true in v6.7)
VGX_DB_DELETE_LOCAL_BACKUPS="true"

# Advanced S3 options (uncomment if needed):
# VGX_DB_S3_REGION="us-west-004"
# VGX_DB_S3_PREFIX="backups/"

# =========================================================================
# CONFIGURATION EXAMPLES & DOCUMENTATION
# =========================================================================
# For complete setup examples and storage backend configurations, see:
# https://github.com/VGXDigital/BackupDB/blob/main/README.md#-storage-backends
#
# Security best practices and troubleshooting:
# https://github.com/VGXDigital/BackupDB/blob/main/README.md#-troubleshooting
# =========================================================================